(() => {
  const state={dashboard:null,universe:null,watch:null,watchSummary:null,operations:null,filter:'ALL',mode:'AUTO',refreshing:false,asking:false,selected:null,order:null,lastNewsCount:null,vezirContext:[]};
  const $=id=>document.getElementById(id);
  const n=v=>{const x=Number(v);return Number.isFinite(x)?x:null};
  const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
  const norm=v=>String(v||'').trim().toLowerCase();
  const money=v=>{const x=n(v);return x===null?'—':`${x<0?'-':''}$${Math.abs(x).toLocaleString('tr-TR',{minimumFractionDigits:2,maximumFractionDigits:2})}`};
  const price=v=>{const x=n(v);return x===null?'—':x.toLocaleString('tr-TR',{maximumFractionDigits:x<1?10:4})};
  const compact=v=>{const x=n(v);return x===null?'—':new Intl.NumberFormat('en-US',{notation:'compact',maximumFractionDigits:2}).format(x)};
  const pct=v=>{const x=n(v);return x===null?'—':`${x>0?'+':''}${x.toFixed(2)}%`};
  const short=v=>{const s=String(v||'').trim();return !s?'—':s.length>18?`${s.slice(0,8)}…${s.slice(-6)}`:s};
  const text=(id,v)=>{const e=$(id);if(e)e.textContent=v};
  const paint=(id,v)=>{const e=$(id);if(!e)return;e.classList.remove('pos','neg','warn');const x=n(v);if(x>0)e.classList.add('pos');else if(x<0)e.classList.add('neg')};
  const getJSON=async(url,opts={})=>{const r=await fetch(url,{cache:'no-store',...opts});const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.detail||`${url} ${r.status}`);return data};

  function openPositions(){return Array.isArray(state.dashboard?.positions)?state.dashboard.positions:[]}
  function positionFor(row){const pool=norm(row?.pool),t0=norm(row?.base_token||row?.token0),t1=norm(row?.token1);return openPositions().find(p=>String(p?.status||'OPEN').toUpperCase()!=='CLOSED'&&((pool&&norm(p.pool)===pool)||(norm(p.token)&&(norm(p.token)===t0||norm(p.token)===t1))))||null}
  function activeRows(source){return openPositions().map(p=>source.find(r=>positionFor(r)?.id===p.id)||{pool:p.pool,base_token:p.token,token0:p.token,display_name:p.symbol,state:'ACTIVE',price_usd:p.current_price??p.entry_price,seismic:{score:p?.entry_evidence?.score??null},_active:p}).filter(Boolean)}
  function radarRows(){const source=state.universe?.available&&Array.isArray(state.universe.rows)?state.universe.rows:[];if(state.filter==='ACTIVE')return activeRows(source);return source.filter(r=>n(r?.seismic?.score)!==null&&n(r.seismic.score)>0).filter(r=>state.filter==='ALL'||String(r.state||'').toUpperCase()===state.filter).sort((a,b)=>(n(b?.seismic?.score)||0)-(n(a?.seismic?.score)||0))}
  function rowName(r){return r?.display_name||r?.base_symbol||short(r?.base_token||r?.token0)||'POOL'}
  function rowState(r,p){return p||r?._active?'ACTIVE':String(r?.state||'COLD').toUpperCase()}
  function stateClass(v){const x=String(v||'').toLowerCase();return ['cold','warm','hot','active'].includes(x)?x:'cold'}

  function renderTop(){const s=state.dashboard?.summary||{};text('balance',money(s.equity));text('dailyPnl',money(s.daily_pnl));paint('dailyPnl',s.daily_pnl);text('dailyDate',s.local_date||'—');text('totalPnl',money(s.total_pnl));paint('totalPnl',s.total_pnl);text('totalPnlPct',s.starting_capital?pct((n(s.total_pnl)||0)/(n(s.starting_capital)||1)*100):'—');text('openCount',String(s.open_count??0));text('openInvestment',money(s.open_investment));text('riskUsed',pct(s.risk_used_pct));text('riskAmount',money(s.open_risk));}

  async function refreshTickers(){try{const rows=await Promise.all(['BTCUSDT','ETHUSDT'].map(s=>getJSON(`https://api.binance.com/api/v3/ticker/24hr?symbol=${s}`)));[['btc',rows[0]],['eth',rows[1]]].forEach(([k,d])=>{text(`${k}Price`,price(d.lastPrice));text(`${k}Change`,pct(d.priceChangePercent));const e=$(`${k}Change`);if(e){e.classList.toggle('pos',n(d.priceChangePercent)>0);e.classList.toggle('neg',n(d.priceChangePercent)<0)}})}catch(e){text('btcChange','VERİ YOK');text('ethChange','VERİ YOK')}}

  function renderRadar(){const body=$('radarBody');if(!body)return;body.innerHTML='';const rows=radarRows();text('candidateCount',state.filter==='ACTIVE'?`${openPositions().length} AÇIK`:`${rows.length} HAREKETLİ`);if(!rows.length){body.innerHTML='<div class="body" style="color:var(--muted);font-size:9px">Bu filtrede uygun hareketli parite yok.</div>';return}for(const r of rows){const p=positionFor(r)||r._active||null,st=rowState(r,p),entry=document.createElement('div');entry.className='radar-entry'+(state.selected===r.pool?' open':'');entry.innerHTML=`<div class="radar-row"><span class="state ${stateClass(st)}">${esc(st)}</span><span class="token-cell"><span><div class="token">${esc(rowName(r))}</div><div class="small">${esc(short(r.pool))}</div></span>${state.mode==='MANUAL'?`<button class="order-btn ${p?'sell':'buy'}" type="button">${p?'SAT':'AL'}</button>`:''}</span><span class="small ${n(r?.seismic?.score)>0?'pos':''}">${n(r?.seismic?.score)===null?'—':n(r.seismic.score).toFixed(2)}</span><span class="small volume-cell">${compact(r.volume_24h_usd)}</span><span class="small price-cell">${price(r.price_usd??p?.current_price)}</span><span class="small change-cell ${n(r.change_5m_pct)>0?'pos':n(r.change_5m_pct)<0?'neg':''}">${pct(r.change_5m_pct)}</span><span class="small liquidity-cell">${compact(r.liquidity_usd)}</span></div><div class="radar-detail"><div class="detail-grid"><div><small>PARİTE</small><b>${esc(rowName(r))}</b></div><div><small>QUOTE</small><b>${esc(String(r.quote_symbol||'—').toUpperCase())}</b></div><div><small>FİYAT</small><b>${price(r.price_usd??p?.current_price)}</b></div><div><small>SCORE</small><b>${n(r?.seismic?.score)===null?'—':n(r.seismic.score).toFixed(2)}</b></div><div><small>5M</small><b>${pct(r.change_5m_pct)}</b></div><div><small>24H HACİM</small><b>${compact(r.volume_24h_usd)}</b></div><div><small>LİKİDİTE</small><b>${compact(r.liquidity_usd)}</b></div><div><small>POOL</small><b title="${esc(r.pool)}">${esc(short(r.pool))}</b></div><div><small>TOKEN</small><b title="${esc(r.base_token||r.token0)}">${esc(short(r.base_token||r.token0))}</b></div>${p?`<div><small>AÇIK POZİSYON</small><b>#${esc(p.id)} · ${money(p.entry_amount_usdt)}</b></div>`:''}</div></div>`;entry.querySelector('.radar-row').onclick=e=>{if(e.target.closest('.order-btn'))return;state.selected=state.selected===r.pool?null:r.pool;renderRadar()};const b=entry.querySelector('.order-btn');if(b)b.onclick=e=>{e.stopPropagation();openTicket(p?'SELL':'BUY',r,p)};body.appendChild(entry)}}

  function setMode(mode){state.mode=mode==='MANUAL'?'MANUAL':'AUTO';document.body.classList.toggle('manual',state.mode==='MANUAL');$('autoModeButton')?.classList.toggle('active',state.mode==='AUTO');$('manualModeButton')?.classList.toggle('active',state.mode==='MANUAL');renderRadar()}
  function setFilter(filter,button){state.filter=filter;document.querySelectorAll('[data-filter]').forEach(x=>x.classList.toggle('active',x===button));state.selected=null;renderRadar()}

  function renderWatch(){const s=state.watchSummary||state.watch?.summary||{};const markValue=s.mark_value_usdt??s.current_value_usdt;const markPnl=s.mark_pnl_usdt??s.pnl_usdt;text('watchCount',String(s.count??0));text('watchOpen',String(s.open??0));text('watchEntry',money(s.entry_usdt_total));text('watchValue',money(markValue));text('watchPnl',money(markPnl));paint('watchPnl',markPnl);const rows=Array.isArray(state.watch?.rows)?state.watch.rows.slice(0,5):[];const body=$('watchRows');if(body)body.innerHTML=rows.length?rows.map(r=>`<tr><td title="${esc(r.token)}">${esc(short(r.token))}</td><td>${money(r.current_value_usdt)}</td><td class="${n(r.pnl_usdt)>=0?'pos':'neg'}">${money(r.pnl_usdt)}</td><td>${esc(r.status||'—')}</td></tr>`).join(''):'<tr><td colspan="4">Kayıt yok</td></tr>'}
  function walletRows(){const i=state.dashboard?.intelligence||{},raw=i?.summary?.wallet_details_json;if(Array.isArray(raw))return raw;if(typeof raw==='string'){try{const p=JSON.parse(raw);return Array.isArray(p)?p:[]}catch(e){return[]}}return[]}
  function renderIntel(){const i=state.dashboard?.intelligence||{};text('walletCount',String(i.tracked_wallets??0));text('whaleCount',String(i.active_whales??0));text('successCount',String(i.successful_wallets??0));const rows=walletRows().slice(0,5),body=$('walletRows');if(body)body.innerHTML=rows.length?rows.map(r=>`<tr><td title="${esc(r.wallet_uid||r.wallet_id||'')}">${esc(short(r.wallet_uid||r.wallet_id))}</td><td>${esc(String(r.success_state||'UNKNOWN').toUpperCase())}</td><td>${esc(String(r.whale_state||'UNKNOWN').toUpperCase())}</td></tr>`).join(''):'<tr><td colspan="3">Detay kaydı yok</td></tr>'}

  function renderNews(){const payload=state.dashboard?.news||{},items=Array.isArray(payload.items)?payload.items:[],calendar=Array.isArray(payload.calendar)?payload.calendar:[];text('newsTitle',`HABER AKIŞI (${items.length})`);text('newsMeta',payload.source||'KAYNAK BAĞLI DEĞİL');const a=$('newsStream'),b=$('calendarStream');if(a)a.innerHTML=items.length?items.map(x=>`<div class="news-item ${x.is_new?'neg':''}">${esc(x.title||x.text||'—')}</div>`).join(''):'<div class="news-empty">Gerçek haber kaynağı panel backend’ine bağlı değil. Sahte haber gösterilmiyor.</div>';if(b)b.innerHTML=calendar.length?calendar.map(x=>`<div class="news-item">${esc(x.title||x.text||'—')}</div>`).join(''):'<div class="news-empty">Gerçek ekonomik takvim kaynağı bağlı değil. Sahte etkinlik gösterilmiyor.</div>'}

  function renderOperations(){const o=state.operations||{},sys=o.system||{};text('systemState',sys.label||'—');const stateName=String(sys.state||'').toUpperCase();const e=$('systemState');if(e)e.className=stateName==='HEALTHY'?'pos':stateName==='DEGRADED'?'warn':'';text('providerState',sys.data_label||'—');}

  function addBubble(textValue,kind='vezir'){const chat=$('chatBody');if(!chat)return;const d=document.createElement('div');d.className=`bubble ${kind==='user'?'user':''}`;d.textContent=textValue;chat.appendChild(d);chat.scrollTop=chat.scrollHeight}
  async function askVezir(question){const input=$('vezirInput'),q=String(question??input?.value??'').trim();if(!q||state.asking)return;state.asking=true;if(input){input.disabled=true;input.value=''}$('vezirSend').disabled=true;addBubble(q,'user');try{const codes={WHY_NO_TRADE:'1',RISK:'2',OPPORTUNITY:'3',WATCH:'4',POSITIONS:'5',SYSTEM:'6',GENERAL:'7'},ctx=state.vezirContext.slice(-4),marker=ctx.length?`\n<<VEZIR_CTX:${ctx.join(',')}>>`:'',wire=q.length+marker.length<=500?q+marker:q;const data=await getJSON('/api/vezir/ask',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:wire})});addBubble(data.answer||'Yanıt alınamadı.');const intent=String(data.intent||data.ai_routed_intent||'').toUpperCase(),code=codes[intent];if(code)state.vezirContext=[...state.vezirContext,code].slice(-4)}catch(e){addBubble(`Bağlantı hatası: ${e.message}`)}finally{state.asking=false;if(input)input.disabled=false;$('vezirSend').disabled=false;input?.focus()}}

  function ensureTicket(){return $('orderModal')}
  function openTicket(side,row,position){state.order={side,row,position,price:n(row.price_usd??position?.current_price??position?.entry_price)};text('ticketTitle',`${side==='BUY'?'AL':'SAT'} · ${rowName(row)}`);text('ticketPrice',price(state.order.price));text('ticketPair',rowName(row));$('buyAmountWrap').style.display=side==='BUY'?'block':'none';$('confirmOrder').textContent=side==='BUY'?'ALIMI ONAYLA':'SATIŞI ONAYLA';$('confirmOrder').className=`confirm ${side==='BUY'?'buy':'sell'}`;text('ticketError','');refreshEstimate();ensureTicket().classList.add('open')}
  function closeTicket(){ensureTicket()?.classList.remove('open');state.order=null}
  function refreshEstimate(){const o=state.order;if(!o)return;const e=$('ticketEstimate');if(o.side==='BUY'){const amount=n($('orderAmount')?.value),tokens=o.price&&amount?amount/o.price:null;e.textContent=`${amount===null?'—':amount.toFixed(2)} USDT → yaklaşık ${tokens===null?'—':tokens.toLocaleString('tr-TR',{maximumFractionDigits:8})} token`}else{const p=o.position||{},tokens=n(p.token_amount)||0,proceeds=o.price?tokens*o.price:null,entry=n(p.entry_amount_usdt)||0,pnl=proceeds===null?null:proceeds-entry;e.textContent=`${tokens.toLocaleString('tr-TR',{maximumFractionDigits:8})} token → yaklaşık ${proceeds===null?'—':proceeds.toFixed(2)} USDT · ham PNL ${pnl===null?'—':pnl.toFixed(2)} USDT`}}
  async function submitOrder(){const o=state.order;if(!o)return;const btn=$('confirmOrder');btn.disabled=true;text('ticketError','');const payload={confirmed:true,side:o.side,pool:o.row?.pool||o.position?.pool||null,token:o.row?.base_token||o.row?.token0||o.position?.token||null,symbol:o.row?.base_symbol||rowName(o.row),position_id:o.position?.id||null};if(o.side==='BUY')payload.amount_usdt=n($('orderAmount')?.value);try{await getJSON('/api/manual-paper/order-v2',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(payload)});closeTicket();await refresh()}catch(e){text('ticketError',e.message)}finally{btn.disabled=false}}

  function clearLiveSnapshot(){state.dashboard=null;state.universe=null;state.watch=null;state.watchSummary=null;state.operations=null;state.selected=null;state.refreshing=false;renderTop();renderRadar();renderWatch();renderIntel();renderNews();renderOperations();text('updatedAt','—')}
  async function refresh(){if(state.refreshing||document.hidden)return;state.refreshing=true;try{const [d,u,w,ws,o]=await Promise.allSettled([getJSON('/api/dashboard'),getJSON('/api/universe-panel'),getJSON('/api/watch-probes'),getJSON('/api/watch-probes-summary-v2'),getJSON('/api/operations-summary')]);if(d.status==='fulfilled')state.dashboard=d.value;if(u.status==='fulfilled')state.universe=u.value;if(w.status==='fulfilled')state.watch=w.value;if(ws.status==='fulfilled')state.watchSummary=ws.value;if(o.status==='fulfilled')state.operations=o.value;renderTop();renderRadar();renderWatch();renderIntel();renderNews();renderOperations();text('updatedAt',new Date().toLocaleTimeString('tr-TR'))}finally{state.refreshing=false}}

  function bind(){$('autoModeButton').onclick=()=>setMode('AUTO');$('manualModeButton').onclick=()=>setMode('MANUAL');document.querySelectorAll('[data-filter]').forEach(b=>b.onclick=()=>setFilter(b.dataset.filter,b));$('vezirSend').onclick=()=>askVezir();$('vezirInput').onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();askVezir()}};document.querySelectorAll('[data-ask]').forEach(b=>b.onclick=()=>askVezir(b.dataset.ask));$('ticketClose').onclick=closeTicket;$('orderModal').onclick=e=>{if(e.target.id==='orderModal')closeTicket()};$('orderAmount').oninput=refreshEstimate;document.querySelectorAll('[data-amount]').forEach(b=>b.onclick=()=>{$('orderAmount').value=b.dataset.amount;refreshEstimate()});$('confirmOrder').onclick=submitOrder;window.addEventListener('focus',refresh);window.addEventListener('pageshow',event=>{if(event.persisted){clearLiveSnapshot();refresh()}});document.addEventListener('visibilitychange',()=>{if(!document.hidden)refresh()})}

  document.addEventListener('DOMContentLoaded',()=>{bind();setMode('AUTO');refreshTickers();refresh();setInterval(refresh,5000);setInterval(refreshTickers,20000)});
})();
